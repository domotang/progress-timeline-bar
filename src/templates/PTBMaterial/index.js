import * as Animations from "./animations";
import * as Components from "./components";
import * as Defaults from "./defaults";

export default function StyledTemplate(styleOptions) {
  return {
    Animations,
    Components,
    Defaults,
    styleOptions
  };
}
